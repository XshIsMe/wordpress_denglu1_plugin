<?php

/**
 * 参数
 */
class PageConfig
{
    const page_title = '登录易设置'; // 选择菜单时要在页面标题标记中显示的文本
    const menu_title = '登录易设置'; // 用于菜单的文本
    const title = '登录易设置';      // 节的格式化标题。显示为该节的标题
    const option_group = 'denglu1_plugin_options'; // 设置组名。应与白名单选项键名称相对应
    const option_name = 'denglu1_plugin_options';  // 要清理和保存的选项的名称（表名）
    const menu_slug = 'denglu1_plugin';            // 显示节的设置页的slug名称
    const id = 'denglu1_plugin_settings';          // 用于标识节的段塞名称。用于标记的“id”属性中
}
